# 모일때 — When2meet UI Renewal

When2meet의 핵심 흐름을 단순하게 유지하면서 한국어 사용성을 개선한 정적 웹 앱입니다.

## 포함 기능

- 이벤트 이름 정의
- 클릭·드래그로 복수 후보 날짜 선택/해제
- 클릭·드래그 방식의 30분 단위 가능 시간표
- `Asia/Seoul` 타임존 고정
- 참가자별 가능 시간 저장과 수정
- 가장 많이 겹치는 시간 자동 표시
- 이벤트·참가자 응답을 포함한 공유 링크
- 데스크톱/모바일 반응형 UI
- 별도 빌드나 외부 라이브러리 없이 실행
